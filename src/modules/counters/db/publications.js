/* MongoDB Collections | planetx-react-materialui-counters-crud */
// @flow

// import { Meteor } from 'meteor/meteor'
// import { Mongo } from 'meteor/mongo'

// import Counters from './collections'

// Meteor.publish({
//   'counters.byId': function (_id: string, title: string): Mongo.Cursor {
//     Counters.upsert({ _id, title }, { $set: { value: 0 } })
//     this.ready()
//     return Counters.find({ _id })
//   },
//   'counters.all': function (): Mongo.Cursor {
//     return Counters.find({})
//   }
// })
